import { Component } from "@angular/core";
import { HttpService } from '../../../../providers/httpService'
import { NavController } from "ionic-angular";


@Component({
    selector: "Revertcode",
    templateUrl: "code.html",
})

export class RevercodePage {
    constructor(
    ) {
    }

}
